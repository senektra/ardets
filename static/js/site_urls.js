/*
 * You can find all the site urls used most commonly throughout the app here. The urls are commented
 * with expected inputs if any (via POST) and expected outputs if any (via POST as a JSON object).
 */

